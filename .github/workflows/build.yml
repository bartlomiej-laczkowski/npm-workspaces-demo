name: Publish Packages

on:
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 12.22.1
          registry-url: https://npm.pkg.github.com/
          always-auth: true
      - run: yarn
      - run: |
          npm install --global npm-cli-login
          TOKEN_CMD="npm-cli-login -u c0d3t0g3th3r -p cm132n4123j4kbnnbdcas -e info@codetogether.com -r https://npm.codetogether.io -s @codetogether"
          $TOKEN_CMD
          cd packages/package-a
          npm login
          npm unpublish --registry https://npm.codetogether.io @dummy/package-a@1.0.0 --force
        env:
          NODE_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}